% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{theme_corporate}
\alias{theme_corporate}
\title{A ggplot2 theme from a corporate design}
\usage{
theme_corporate(design, base_size = NULL)
}
\arguments{
\item{design}{A \code{corporate_design} object.}

\item{base_size}{Optional base font size in points. Defaults to the design's
axis size.}
}
\value{
A ggplot2 theme object.
}
\description{
Builds a complete \code{\link[ggplot2:theme]{ggplot2::theme()}} from the design's typography, semantic
colours and geometry tokens. Used on its own for a single figure, or set as
the global default by \code{\link[=set_corporate_design]{set_corporate_design()}}.
}
\examples{
design <- corporate_design(
  system.file("designs/efk.yml", package = "CorporateDown")
)
library(ggplot2)
p <- ggplot(mpg, aes(displ, hwy)) +
  geom_point() +
  theme_corporate(design)
# print(p) on a ragg device to render the corporate design

}

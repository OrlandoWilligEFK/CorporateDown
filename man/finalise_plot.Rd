% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/finalise.R
\name{finalise_plot}
\alias{finalise_plot}
\title{Finalise and export a figure in the corporate design}
\usage{
finalise_plot(
  plot,
  title = NULL,
  subtitle = NULL,
  source = NULL,
  logo = NULL,
  save_path = NULL,
  width = 8,
  height = 5,
  dpi = 300,
  place_logo = TRUE,
  design = NULL
)
}
\arguments{
\item{plot}{A ggplot object.}

\item{title, subtitle}{Optional title / subtitle overrides.}

\item{source}{Optional source line, placed as the plot caption.}

\item{logo}{Optional path to a logo. Set to \code{NULL} to skip; defaults to the
design's logo when one is configured and found.}

\item{save_path}{Optional output path (\code{.png}). If \code{NULL}, the annotated
plot object is returned instead of being written.}

\item{width, height}{Output size in inches.}

\item{dpi}{Output resolution.}

\item{place_logo}{Whether to place the logo. Defaults to \code{TRUE}.}

\item{design}{A \code{corporate_design} object. Defaults to the active design.}
}
\value{
The \code{save_path} (invisibly) when writing, otherwise the annotated
plot object.
}
\description{
Adds an optional title, subtitle and source line, optionally places the
logo (see \code{\link[=add_logo]{add_logo()}}) and, if \code{save_path} is given, writes the figure with
the \code{ragg} PNG device at the requested size and resolution. The background
colour is taken from the design.
}
\examples{
\dontrun{
set_corporate_design("efk")
library(ggplot2)
p <- ggplot(mpg, aes(class, fill = drv)) + geom_bar()
finalise_plot(p, source = "Quelle: EFK", save_path = "figure.png")
}

}

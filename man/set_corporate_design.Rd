% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply.R
\name{set_corporate_design}
\alias{set_corporate_design}
\title{Activate a corporate design globally}
\usage{
set_corporate_design(design)
}
\arguments{
\item{design}{A \code{corporate_design} object, a path to a YAML file, or the
name of a shipped design (e.g. \code{"efk"}).}
}
\value{
The activated design (invisibly).
}
\description{
Registers the design across the idiomatic ggplot2 mechanisms so that
ordinary ggplot2 code renders in the corporate design without any further
theme or scale calls:
}
\details{
\itemize{
\item the theme via \code{\link[ggplot2:theme_get]{ggplot2::theme_set()}},
\item default discrete and continuous colour/fill scales via \code{options()}
(\code{ggplot2.discrete.colour}, \code{ggplot2.discrete.fill},
\code{ggplot2.continuous.colour}, \code{ggplot2.continuous.fill}),
\item per-geom default colours and fonts via \code{\link[ggplot2:update_defaults]{ggplot2::update_geom_defaults()}},
\item fonts via \code{\link[=register_design_fonts]{register_design_fonts()}}.
}

The previous theme, options and geom defaults are stored so
\code{\link[=reset_corporate_design]{reset_corporate_design()}} can restore them.
}
\examples{
\dontrun{
set_corporate_design("efk")
library(ggplot2)
ggplot(mpg, aes(class, fill = drv)) + geom_bar()  # in the corporate design
reset_corporate_design()
}

}
